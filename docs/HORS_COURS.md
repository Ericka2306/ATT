# Notions hors cours utilisées dans le projet

Règle R7 : toute notion non enseignée dans le module M1 (voir `docs/references/SYNTHESE_COURS_M1.md` §4) est ajoutée ici **avant** d'être utilisée, avec une explication simple compréhensible par le binôme et rattachée à ce qui a été vu en cours.

| # | Notion | Étape | Pourquoi on en a besoin | Explication simple | Décision |
|---|---|---|---|---|---|
| 1 | Catalogue de versions `libs.versions.toml` | 0 | généré par Android Studio, évite de répéter les versions | même chose que les chaînes `"2.8.0"` du cours, rangées dans un fichier unique | accepté |
| 2 | Kotlin intégré à AGP 9, plugin Compose 2.2.10, KSP 2.3.12 | B0 | le squelette est en AGP 9.3.2 (le cours en 8.x) | AGP 9 embarque le compilateur Kotlin (2.2.10) ; le plugin Compose suit cette version ; KSP 2.3.x est indépendant de Kotlin et compatible avec le Kotlin intégré | accepté, **confirmé par le build vert du 15/09/2026** (Room 2.8.5, Navigation 2.10.1, Lifecycle 2.11.0, BOM 2026.08.00, activity-compose 1.13.0) |
| 3 | `@ForeignKey`, `@Index` sur les entités Room | 2 | ~20 tables liées, intégrité des identifiants | le `FOREIGN KEY` / `INDEX` de SQL, posé en annotation comme `@Query` | accepté 15/09/2026 |
| 4 | `db.withTransaction { … }` (room-ktx) | B2/C2 | écrire une modification et son `Historique` ensemble | `BEGIN … COMMIT` de SQL : tout ce qui est dans le bloc est enregistré ensemble, ou rien ; fonction `suspend`, s'appelle dans `viewModelScope.launch` comme les DAO | accepté 15/09/2026 |
| 5 | `enum class` persisté par Room | 2 | statuts (dossier, session, présence…) | Room enregistre le nom de l'enum en texte ; on relit `Statut.valueOf` automatiquement | accepté 15/09/2026 |
| 6 | `OutlinedTextField` + `KeyboardOptions` | 5 | formulaires (candidat, session, notes) | un `Text` dont la valeur est un état et qui appelle `onValueChange`, comme le compteur du TP4 | accepté 15/09/2026 |
| 7 | `Scaffold` / `TopAppBar` / `FloatingActionButton` | C1 | ~15 écrans, barre de titre et bouton d'action | un `Column` Material avec des emplacements nommés (barre, contenu, bouton) ; `TopAppBar` exige l'annotation `@OptIn(ExperimentalMaterial3Api::class)` car Google marque encore cette API « expérimentale » (elle est stable en pratique) | accepté 15/09/2026 (rester sobre) |
| 8 | Hachage PBKDF2 + sel (`SecretKeyFactory`, `SecureRandom`) | C1 | authentification locale sans mot de passe en clair | fonction à sens unique, volontairement lente, de la bibliothèque Java standard ; on compare des empreintes, pas des mots de passe | accepté 15/09/2026 (minSdk 26) |
| 9 | Fonctions pures dans `metier/` + tests JUnit | 6 à 13 | prouver le calcul selon barème et les contrôles de planification | des fonctions Kotlin sans Android, comme `Collectes.kt` du TP1, exécutées par JUnit | accepté 15/09/2026 |
| 10 | Impression Android (`PrintManager`, `WebView` HTML ou `PdfDocument`) | 14 | convocations et listes imprimables (cadrage §6, §10) | on génère une page HTML et on la confie au service d'impression du téléphone | accepté 15/09/2026 |
| 11 | `navigate(route) { popUpTo(...) { inclusive = true } }` | C1 | après connexion et déconnexion, le bouton « retour » ne doit pas ramener sur l'écran précédent | `navigate` comme en S5, avec une option qui vide d'abord la pile jusqu'à l'écran indiqué (comme `popBackStack` répété) | accepté 15/09/2026 |
| 12 | `PasswordVisualTransformation` | C1 | masquer le mot de passe saisi | option du `OutlinedTextField` qui affiche des points à la place des caractères | accepté 15/09/2026 |
| 13 | `MaterialTheme(colorScheme = …)` avec une palette maison (`ui/theme/Theme.kt`) | C1 | identité visuelle ATT (bleu, vert, ambre) | le même `MaterialTheme { }` que le cours, auquel on passe nos couleurs au lieu des couleurs par défaut ; les écrans continuent d'utiliser `MaterialTheme.colorScheme.primary` | demandé par le dev 1 le 15/09/2026 |
| 14 | `Icon` + `Icons.Filled.*` (jeu d'icônes de base fourni avec material3), `IconButton` | C1 | icônes des cartes du menu, flèche retour, déconnexion | une image vectorielle prête à l'emploi affichée comme un `Text` ; `IconButton` = un `Button` rond sans texte | demandé par le dev 1 |
| 15 | `Image(painterResource(R.drawable.…))`, `ContentScale.Crop`, `Brush.verticalGradient` | C1 | photo de la RN44 et logo sur l'écran de connexion | afficher une image des ressources ; `Crop` la recadre pour remplir la zone ; le dégradé est un voile de couleur pour la lisibilité | demandé par le dev 1 |
| 17 | `DropdownMenu` / `DropdownMenuItem` | C2 | choisir une région parmi 24 (trop pour des `FilterChip`) | une liste déroulante ancrée à un bouton ; ouvert/fermé est un `remember { mutableStateOf(false) }` local (S4), le choix remonte au ViewModel | à valider par le binôme |
| 18 | `flatMapLatest` sur un `Flow` | C2 | l'écran de détail dépend d'un identifiant qui change (`idDetail`) | « quand l'identifiant change, on abandonne l'ancien flux Room et on écoute le nouveau » ; cousin de `combine` vu en S7 | à valider par le binôme |
| 19 | `BuildConfig.DEBUG` (`buildFeatures.buildConfig = true`) | C2 | afficher les comptes de test sur l'écran de connexion sans risquer de les livrer | Gradle génère une classe `BuildConfig` avec un booléen `DEBUG` vrai en développement, faux en release : le bloc disparaît tout seul de la version finale | demandé par le dev 1 le 15/09/2026 |
| 20 | `Checkbox` | C3 | pièces du dossier fournies / non fournies | une case à cocher : même principe que `Switch` (S7), l'état vit dans le ViewModel | demandé (C3) |
| 21 | `inline fun <reified VM>` (fonction générique) | C3 | un seul utilitaire `viewModelDuSousParcours` pour tous les sous-parcours au lieu de le recopier | « fonction à trou de type » : on écrit une fois la recette, Kotlin la spécialise pour chaque ViewModel | factorisation demandée par le dev 1 |
| 22 | `modifierFormulaire { it.copy(...) }` dans les ViewModels | C3 | remplace six fonctions `changerX` par une seule | c'est le `update { it.copy() }` de S6, exposé une fois au lieu d'une fois par champ ; l'UI ne fait toujours que « signaler » | factorisation demandée par le dev 1 |
| 23 | `kotlin.random.Random` passé en paramètre (`Random(graine)`) | C9 | tirage au sort du sujet et des propositions, testable | le hasard est un « service » qu'on donne à la fonction : en test on passe une graine fixe, le résultat est reproductible ; en production `Random.Default` | à valider par le binôme |
| 24 | `AndroidView` (afficher une `WebView` dans Compose) et `PrintManager` | C13 | aperçu et impression des convocations, listes et relevés | `AndroidView` insère une vue Android classique (ici la `WebView` qui affiche notre page HTML) dans un écran Compose : c'est le seul moyen, Compose n'ayant pas de composant d'affichage HTML. `PrintManager` est le service d'impression du téléphone : on lui remet la page, il propose l'imprimante ou l'enregistrement en PDF. Aucun réseau. | accepté (C13), voir aussi la ligne 10 |
| 25 | `LazyVerticalGrid` / `GridCells.Fixed(2)` | D2a | menu de l'accueil en tuiles sur deux colonnes | la `LazyColumn` du cours, mais qui range ses éléments en colonnes ; `GridItemSpan(2)` pour une tuile pleine largeur (l'en-tête) | demandé par le dev 1 (design, 18/09/2026) |
| 26 | `Shapes` et `Typography` passés à `MaterialTheme`, `OutlinedTextFieldDefaults.colors`, `border = BorderStroke` sur `Card` | D2a | coins arrondis, titres affirmés, champs blancs à bord fin, cartes sans ombre | les mêmes réglages que le cours (`MaterialTheme`, `Card`, `OutlinedTextField`), avec des valeurs fournies au lieu des valeurs par défaut | demandé par le dev 1 (design) |
| 27 | `FlowRow` (`ExperimentalLayoutApi`) | D2a | rangées de boutons (dossier, appel, session, résultat) sur un petit écran | un `Row` qui passe à la ligne quand la largeur manque ; `LigneActions` dans `ui/communs/Champs.kt` | demandé par le dev 1 (responsivité, 18/09/2026) |
| 16 | `verticalScroll(rememberScrollState())`, `statusBarsPadding()` | C1 | l'écran de connexion doit défiler quand le clavier est ouvert ; le titre ne doit pas passer sous la barre d'état | rend un `Column` défilable (comme `LazyColumn` mais pour un contenu fixe) ; ajoute la hauteur de la barre d'état en marge | demandé par le dev 1 |

Ajouter une ligne à chaque nouvelle notion. Une notion refusée par le binôme est remplacée par l'équivalent vu en cours.
